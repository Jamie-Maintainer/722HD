apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-green
  namespace: production
  labels:
    app: frontend
    slot: green
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
      slot: green
  template:
    metadata:
      labels:
        app: frontend
        slot: green
    spec:
      containers:
        - name: frontend
          image: nginx:1.27-alpine
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10

---
# Internal ClusterIP used for pre-switch smoke testing.
# Production traffic never uses this service.
apiVersion: v1
kind: Service
metadata:
  name: frontend-green
  namespace: production
  labels:
    app: frontend
    slot: green
    role: smoke
spec:
  type: ClusterIP
  selector:
    app: frontend
    slot: green
  ports:
    - port: 80
      targetPort: 80
